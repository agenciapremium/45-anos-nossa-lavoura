Text field for lead forms and search; label is uppercase micro-type, field carries an 8px chamfer.

```jsx
<Input label="WhatsApp" placeholder="(69) 90000-0000" hint="Retornamos em até 1 dia útil." />
<Input label="E-mail" error="Informe um e-mail válido." />
```

Focus is a 2px lima border (plus the global focus ring); never remove the ring.

Consent and multi-select control; the 22px box carries a 6px chamfer and fills lima when checked.

```jsx
<Checkbox label="Aceito receber ofertas no WhatsApp" checked={ok} onChange={e => setOk(e.target.checked)} />
```

Single-choice group — the one place the full eight-sided octagon shows all its edges.

```jsx
<Radio name="fase" value={v} onChange={setV} options={['Cria', 'Recria', 'Engorda']} direction="row" />
```

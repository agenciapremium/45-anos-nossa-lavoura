Dropdown matching Input's chamfer and border logic; the arrow is the brand seta, never a generic chevron.

```jsx
<Select label="Rebanho" placeholder="Selecione" options={['Corte', 'Leite', 'Cria']} />
```

Section switcher for catalogue and product-detail views; top corners are cut so the tab reads as an octagon rising out of the rule.

```jsx
<Tabs items={['Bovinos', 'Equinos', 'Aves']} value={tab} onChange={setTab} />
<Tabs tone="dark" items={tabs} value={tab} onChange={setTab} />
```

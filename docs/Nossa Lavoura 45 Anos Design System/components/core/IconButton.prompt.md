Icon-only chamfered button for carousels, close actions and compact toolbars.

```jsx
<IconButton icon="seta" label="Próximo" />
<IconButton icon="seta" iconRotate={180} variant="outline" label="Anterior" />
```

Always pass `label`. Minimum 44px hit target: use `size="md"` or larger on touch surfaces.

Short confirmation after a form submit or cart action; terra by default, semantic fills for success/danger.

```jsx
<Toast title="Pedido enviado" message="Nosso consultor entra em contato hoje." />
```
